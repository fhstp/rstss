[workspace]
members = ["spec2parse"]
resolver = "3"

[workspace.package]
edition = "2024"

[profile.dev]

[profile.release]
debug = 0   # faster builds, but no backtraces in debugger

[workspace.dependencies]
# local crates
spec2parse = { path = "spec2parse"}

# non-local crates
